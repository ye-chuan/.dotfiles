" HTML Indentation 2 Spaces (as per Google's Style Guide)
setlocal tabstop=2       " Width of `\t` character
setlocal softtabstop=2   " Width of a "Tab" key (and Backspace)
setlocal shiftwidth=2    " Width of a level of indent (e.g. >>)
